# Local Development Profile
spring.profiles.active=dev

# Development Database Configuration - PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/rerover
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080
server.servlet.context-path=/

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Session Management
server.servlet.session.timeout=30m
server.servlet.session.cookie.name=JSESSIONID
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.tracking-modes=COOKIE

# Disable Spring Session
spring.session.store-type=none
spring.session.jdbc.initialize-schema=never
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.session.SessionAutoConfiguration

# Allow Hibernate to generate optimized SQL
spring.jpa.properties.hibernate.id.new_generator_mappings=true
spring.jpa.properties.hibernate.connection.characterEncoding=UTF-8
spring.jpa.properties.hibernate.connection.useUnicode=true

# JWT Configuration
# app.jwt.secret=VGhpcyBpcyBhIHNlY3VyaXR5IGtleSBmb3IgSldUIHRva2VuIGdlbmVyYXRpb24uIEl0IG5lZWRzIHRvIGJlIGF0IGxlYXN0IDY0IGJ5dGVzIGZvciBINTEyLg==
# app.jwt.expiration=86400000
# app.jwt.header=Authorization
# app.jwt.token-prefix=Bearer
# app.jwt.token-validity=86400000

# File Upload Configuration
file.upload-dir=uploads/
